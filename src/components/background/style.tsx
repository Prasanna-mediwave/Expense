import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  image: {
    flex: 1,
    resizeMode: 'cover',
  },
  maincontainer: {
    paddingTop: 36,
    paddingHorizontal: 24,
    marginBottom: 80,
  },
});
